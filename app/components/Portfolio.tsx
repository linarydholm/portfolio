import { redHatRegular } from '@/styles/fonts/fonts';

export function Portfolio() {
  return (
    <section className="px-6 py-24 bg-dark-bg bg-gradient-to-b from-transparent to-tertiary-20">
      <div className="max-w-7xl m-auto">
        <p className={`${redHatRegular.className} text-l pb-1`}>Portfolio</p>
        <p className="text-xs">Coming soon</p>
      </div>
    </section>
  );
}
